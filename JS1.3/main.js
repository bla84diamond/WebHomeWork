// 1.Сформировать массив из 15 целых чисел, выбранных случайным образом из интервала[-10, 30].Найти среднее арифметическое положительных элементов.
console.log('1.Сформировать массив из 15 целых чисел, выбранных случайным образом из интервала[-10, 30].Найти среднее арифметическое положительных элементов.');
// Создаем произвольный массив длиной 15 от [-10 до 30]
const arr = Array.from({ length: 15 }, () => Math.floor(Math.random() * 41) - 10);
console.log(arr)

// Получаем только положительные числа в массиве.
const positiveArr = arr.filter(num => num > 0);
console.log(positiveArr)

//Среденее арифмитическое
const mean = positiveArr.reduce((acc, curr) => acc + curr, 0) / positiveArr.length;
console.log(mean)


// 2.Заменить отрицательные элементы в числовом массиве из n чисел (n>10) их квадратами, оставив остальные без изменения.
//console.log('');
console.log('2.Заменить отрицательные элементы в числовом массиве из n чисел (n>10) их квадратами, оставив остальные без изменения.');
const arr2 = []
for (let i = 0; i < arr.length; i++) {
    if (arr[i] < 0) {
        arr2[i] = arr[i] * arr[i];
    }
    arr2[i] = arr[i]    
}

console.log(arr2);

// 3.В заданном массиве найти среднее арифметическое положительных чисел, среднее арифметическое отрицательных чисел и число нулей.
console.log('3.В заданном массиве найти среднее арифметическое положительных чисел, среднее арифметическое отрицательных чисел и число нулей.');
let positiveMean = 0, negativeMean = 0, zeroCount = 0;
let positiveCount = 0, negativeCount = 0;
for (let i = 0; i < arr.length; i++) {
    if (arr[i] > 0) {
        positiveMean += arr[i];
        positiveCount++;
    } else if (arr[i] < 0) {
        negativeMean += arr[i];
        negativeCount++;
    } else {
        zeroCount++;
    }
}
console.log('Нулей:');
 console.log(zeroCount)
positiveMean /= positiveCount;
console.log('Среднее положительных');
 console.log(positiveMean)
negativeMean /= negativeCount;
console.log('Среднее отрицательных');
 console.log(negativeMean)


// 4.Организовать симулятор, который выдает только три случайных значения – красный, черный и белый (0, 1, 2). Запустить симуляцию 1000 000 раз. Узнать, какое максимальное число раз выпадало красное значение.
console.log('4.Организовать симулятор, который выдает только три случайных значения – красный, черный и белый (0, 1, 2). Запустить симуляцию 1000 000 раз. Узнать, какое максимальное число раз выпадало красное значение.');
// Создаем массив для хранения последовательности значений
let sequence = [];
// Создаем переменные для хранения текущей последовательности и максимальной последовательности
let current = 1;
let max = 1;
// Запускаем симуляцию 1000000 раз
for (let i = 0; i < 1000000; i++) {
  // Генерируем случайное число от 0 до 2
  let random = Math.floor(Math.random() * 3);
  // Добавляем его в массив
  sequence.push(random);
  // Если предыдущее значение равно текущему, увеличиваем текущую последовательность
  if (sequence[i] === sequence[i - 1]) {
    current++;
    // Если текущая последовательность больше максимальной, обновляем максимальную
    if (current > max) {
      max = current;
    }
  } else {
    // Если предыдущее значение не равно текущему, сбрасываем текущую последовательность
    current = 1;
  }
}
// Выводим результат
console.log(`Самая длинная последовательность: ${max}`);
